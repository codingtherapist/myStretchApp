<%- include('partials/header') -%>


<main class="container">
  <h1 class="mb-5 mt-5"> My Stretch Collection</h1>
  <p><strong>User Name</strong>: <%= user.userName %></p>
  <p><strong>Email</strong>: <%= user.email %></p>
    <div class="row justify-content-center">
      <h2>Welcome to your stretch collection!</h2>
      <p>Upload photos of stretches you find online and make your own stretch routine collection.<br> This is also a great place to upload photos of you in your own poses to track progress and use as a daily/ weekly stretch journal.</span></p>
      <div class="col-6">
          <div class="mt-5">
            <h2>Add a photo</h2>
            <form action="/post/createPost" enctype="multipart/form-data" method="POST">
              <div class="mb-3">
                <label for="imgUpload" class="form-label">Image</label>
                <input type="file" class="form-control" id="imageUpload" name="file">
              </div>
              <div class="mb-3">
                <label for="caption" class="form-label">Caption</label>
                <textarea class="form-control" id="caption" name="caption"></textarea>
              </div>
              
              <button type="submit" class="btn btn-outline-secondary" value="Upload">Submit</button>
            </form><br><br>
            Note: This web app is in beta testing. 
            
          </div>
      </div>
     
     <!-- <h2>Add a comment</h2>
      <form action="/comment/createComment/" method="POST">
        <div class="mb-3">
            <label for="comment" class="form-label">Comment</label>
            <input type="text" class="form-control" id="comment" name="comment">
        </div>
        <button type="submit" class="btn btn-outline-secondary">Submit</button>
      </form>-->
        <!--<div class="mt-1">
        <h2>Add a comment</h2>
        <form action="/comment/createComment/" method="POST">
          <div class="mb-3">
              <label for="comment" class="form-label">Comment</label>
              <input type="text" class="form-control" id="comment" name="comment">
          </div>
          <button type="submit" class="btn btn-outline-secondary">Submit</button>
        </form>
      </div>-->
     
    
    

  
      <div class="col-8">
     
        <ul class="row list-unstyled">
          <br><br> 
          <h2 class="centered"></h2>
        <% for(var i=0; i<posts.length; i++) {%>
          <div class="col-10 justify-content-between mt-5 border p-3 mb-2 bg-light text-dark">
          
            <li>   <div class="col-12 mt-1 text-center">
          
              <p>Date: <%= posts[i].createdAt.toLocaleDateString("en-US")%></p>
            </div></li>
            <li class="col-6 justify-content-between mt-5">
            <a href="/post/<%= posts[i]._id%>">
              <img class="img-fluid" src="<%= posts[i].image%>">
            </a>
          </li>
          <li>   <div class="col-12 mt-1 text-center">
            <p>"<%= posts[i].caption%>"</p>
            
          </div></li>
          </div>
        <% } %>
        </ul> 
      </div>  
    </div>

  </main>

<%- include('partials/footer') -%>